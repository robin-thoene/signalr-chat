import React, { FunctionComponent, ReactElement } from 'react';

/**
 * Displays data in a bar chart.
 *
 * @returns {ReactElement} The bar chart component.
 */
const BarChart: FunctionComponent = (): ReactElement => {
    return <></>;
};

export default BarChart;
