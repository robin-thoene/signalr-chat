/**
 * Properties of the top navigation component.
 */
interface INavigationTopProps {
    /** Whether to enable a global search bar or not. */
    enableGlobalSearch?: boolean;
}

export type { INavigationTopProps };
