import React, { FunctionComponent, ReactElement } from 'react';

/**
 * Navigation menu placed on the bottom of the screen.
 *
 * @returns {ReactElement} The bottom navigation menu component.
 */
const NavigationBottom: FunctionComponent = (): ReactElement => {
    return <></>;
};

export default NavigationBottom;
