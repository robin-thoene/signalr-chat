import React, { FunctionComponent, ReactElement } from 'react';

/**
 * Menu that renders multiple view under multiple tabs.
 * @returns {ReactElement} The tab menu component.
 */
const TabMenu: FunctionComponent = (): ReactElement => {
    return <></>;
};

export default TabMenu;
