import React, { FunctionComponent, ReactElement } from 'react';

/**
 * Displays multiple slides of data in a carousel.
 * @returns {ReactElement} The carousel component.
 */
const Carousel: FunctionComponent = (): ReactElement => {
    return <></>;
};

export default Carousel;
