import React, { FunctionComponent, ReactElement } from 'react';

/**
 * Basic list of items, that can be numbered or not numbered.
 * @returns {ReactElement} The list component.
 */
const List: FunctionComponent = (): ReactElement => {
    return <></>;
};

export default List;
